spring:
  application:
    name: segmentation-configuration
  cloud:
    config:
      enabled: false
      discovery:
        enabled: false
  jpa:
    hibernate:
      ddl-auto: create

eureka:
  client:
    enabled: false

embedded:
  postgres:
    enabled: true

local:
  management:
    port: 8051
  server:
    port: 8050

